//import the package required
/**
 * Write a description of class ConsoleInput here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class ConsoleInput
{
   public static void main (String [] args) {
   //Declare the Scanner variable
   
   //Declare a String variable called studentName
   
   //Declare an integer variable called age and initialize it with 0
   
   //Declare a double variable called ci and initialize it with 0
   
   //Ask the user to type his full name
   
   //Read from the keyboard and assign the value to the studentName variable
   
   
   //Ask the user to type his age
   
   //Test if the input to be acceped is an integer
       //if yes : read an integer from the keyboard and assign the value to the age variable
       //if not : display a message and flush the wrong input
       
       
   //Ask the user to type his ci
   
   //Test if the input to be acceped is a double
       //if yes : read a double from the keyboard and assign the value to the ci variable
       //if not : display a message and flush the wrong input
   }
}
